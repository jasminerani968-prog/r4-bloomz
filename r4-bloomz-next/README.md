# R4 Bloomz – Next.js App

An eCommerce-styled front-end for R4 Bloomz with Tailwind and lightweight UI components.

## Quick start

```bash
npm install
npm run dev
# open http://localhost:3000
```

## Deploy
- Easiest: Import this folder into https://vercel.com and click Deploy.
- Or run `npm run build && npm start` on your own server.

## Notes
- Images use Unsplash placeholders. Replace with your product photos.
- WhatsApp CTA is wired; replace the phone number in `page.tsx`.
- Cart/Wishlist are client-side only (no backend). Connect to Shopify/Stripe later.
